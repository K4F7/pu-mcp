{
  "scripts": {
    "build": "tsc --noEmit && bun build --target=browser --minify --format=esm --outdir=src/pu_tool/web_static src/pu_tool/web_static/app.ts src/pu_tool/web_static/reminders.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "fullcalendar": "7.0.0",
    "temporal-polyfill": "1.0.1"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  }
}

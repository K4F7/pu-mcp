# PU 二课活动

本地工具：用本人 PU 账号给 agent 访问口袋校园（列活动、详情、已报、报名），并给出各活动类型已签到次数。登录只走 CLI `pu login`；学校 sid 可经学校查询用中文校名查到。登录配置用 skill，选活动和学分不算 skill。不保存待抢，不推荐场次；何时报、报哪场由 agent 决定。有效学分由用户自己按 glossary 折。CLI 和 MCP 运行时都不折算，不写进 skill 或工具描述。学时只作参考。不登录教务，不接入学工。CLI 与 MCP 同一套能力（登录只在 CLI）。本分支是 CLI / MCP；原始网页留在 `main`。不提供待抢命令。

## Language

**二课学分**:
学校认定的第二课堂学分，与专业课学分分开计算。已获由用户按各活动类型公式从已签到次数折，不是教务实时认定，本工具不代算。
_Avoid_: 学分（单独使用）, 素质学分, 素拓学分, 创新学分, academic_credit（对用户）

**专业课学分**:
课程教学学分。本工具不处理。
_Avoid_: 课程学分（当需要与二课学分区分时）

**学分预警**:
教务平台展示二课学分已获、应获、认定标准和是否达标的视图。本工具只用它上面的规则（认定标准、有效上限、阶段线），不把它当实时数据源。

**板块**:
二课学分的分类桶。名称以教务「学分预警」为准。
_Avoid_: 类别, 分类, 校园阅读（教务名是蛟湖阅读）, 活动类型（那是 PU 侧的标签）

**蛟湖阅读**:
教务硬性板块，但是独立任务，不通过 PU 活动完成。本工具不算它的缺口，也不用它筛 PU 活动。

**创新创业类**:
教务硬性板块，通过学工平台认定。PU 上没有这类活动可抢，不是「有活动但不计有效分」。本工具不处理该板块缺口。

**志愿服务**:
教务硬性板块，通过学工平台认定。PU 上没有这类活动可抢，不是「有活动但不计有效分」。本工具不处理该板块缺口。
_Avoid_: 志愿公益

**学工平台**:
认定创新创业类、志愿服务等的系统。这些板块的活动不在 PU 上。本工具不接入。

**硬性板块**:
教务规定了至少应获学分、必须达标的板块：蛟湖阅读、创新创业类、志愿服务、思想引领、学术讲座。
_Avoid_: 弹性板块

**缺口范围**:
本工具只计算能用 PU 活动补的硬性板块：思想引领、学术讲座。蛟湖阅读、创新创业类、志愿服务归学工或独立任务，排除在外。

**必须补**:
当前仅学术讲座，直到有效学分达到认定标准 0.5。思想引领已达标，只跟踪不作为必须项。

**弹性板块**:
无单项必须达标，只向总有效分贡献（如校园文化）。普通签到到达该类型上限后，再刷 PU 活动不增加总有效分。
_Avoid_: 硬性板块

**板块达标**:
缺口范围内的硬性板块，有效学分满足认定标准。未达标就必须补，即使总有效分已过阶段线和最终 6 分。

**认定标准**:
缺口范围内硬性板块的达标线。学术讲座为有效学分 0.5，思想引领为 1。

**已获**:
以 PU 已签到次数为准，用户按次 × 0.1、再套普通签到上限和模块上限自己折。本工具不计算、不返回已获。仅报名不计。允许和教务已认定有时差。
_Avoid_: 教务已获（本工具不拉取）

**有效学分**:
单类型里能用来达标、并计入总有效分的二课学分。受普通签到上限和模块上限约束。
_Avoid_: 总学分, 总分

**总有效分**:
各活动类型有效学分之和。阶段线和最终 6 分跟这个走。数字来自 PU 已签到记录，不手填。
_Avoid_: 总学分, 总分

**总学分**:
本工具不用它判断阶段线或最终 6 分。
_Avoid_: 总有效分, 有效学分

**普通签到**:
PU 活动参加一次这种认定方式。除学术讲座外，本工具清单内类型用这种方式最多 1 有效学分；学术讲座没有这个 1 分子上限。
_Avoid_: 普通参与活动, 参加活动（单独使用）

**认定项目**:
获奖等非普通签到的认定方式，在学工平台查看。本工具不获取、不计入、不推荐「去获奖」。
_Avoid_: 普通签到

**普通签到上限**:
思想引领、校园文化、社会实践、学科竞赛、体育健身：普通签到最多 1 有效学分（10 次）。学术讲座：无此上限。

**模块上限**:
该活动类型有效学分绝对上限 2（含学工拿奖）。本工具只能做到普通签到能贡献的部分。

**次**:
计量单位。每个活动签到后计 0.1 有效学分，不论学时长短。再套该类型的普通签到上限和模块上限。仅报名不计。
_Avoid_: 学时（学时只作参考）

**已签到次数**:
按活动类型，PU 状态为已签到的场次数。MCP 给 agent 的进度只给这个。报名未签到不计。
_Avoid_: 已获（当作为 MCP 返回）, 参加次数（当含仅报名）

**签到**:
计次时点。签到后才计入有效学分；仅报名不计。
_Avoid_: 报名（报名不是计次）

**PU 状态**:
是否已签到、能否计 0.1，只认 PU 回写，不认本地手勾。

**思想引领**:
0.1 / 次，普通签到上限 1，模块上限 2。多出的 1 靠拿奖，PU 做不了。当前记 1，已达标。

**校园文化**:
0.1 / 次，普通签到上限 1，模块上限 2。多出的 1 靠拿奖，PU 做不了。当前记 1。

**学术讲座**:
0.1 / 次，模块上限 2，无普通签到 1 分子上限。必须补到认定标准 0.5。当前记 0.3（3 次）。

**社会实践**:
0.1 / 次，普通签到上限 1，模块上限 2。当前 0。

**学科竞赛**:
0.1 / 次，普通签到上限 1，模块上限 2。当前 0。

**体育健身**:
0.1 / 次，普通签到上限 1，模块上限 2。当前 0。

**阶段线**:
按年级学期的累计最低总有效分：大一下 4 月前 1，大二上 9 月前 2，大二下 4 月前 3，大三上 9 月前 4，大三下 4 月前 5，大四上 9 月前 6。

**学时**:
PU「我的成绩」里按活动累计的分。只作参考，不参与有效分计算。
_Avoid_: 学分, 二课学分, 成绩（单独使用）, 次

**活动类**:
PU 学时下对应普通签到的那一类，和成果类并列。

**成果类**:
PU「我的成绩」里与活动类并列的入口。获奖等认定项目走学工，本工具暂不拉取成果类。

**活动类型**:
PU 给每场活动打的类型标签。PU 上会有、本工具会计已签到次数的是：社会实践、校园文化、思想引领、学科竞赛、学术讲座、体育健身。志愿服务、创新创业的活动不在 PU 上。
_Avoid_: 板块, 志愿公益

**学校 sid**:
PU 给学校的数字 id。登录请求用它，不是登录成功后返回、和 token 一起放进 Bearer 的那个 sid。可用中文校名经学校查询得到；`pu login` 仍只收 sid，不收校名。
_Avoid_: 学校名（当作为 pu login 参数）, 会话 sid

**学校查询**:
按中文校名（或关键字）查出学校 sid。不登录可调。登录命令不内置校名。
_Avoid_: 把校名当作 pu login 的参数

**登录**:
用学校 sid、账号、密码向 PU 换会话。只走 CLI `pu login`。MCP 不收账密、不打开选学校页。明文密码不落盘。
_Avoid_: MCP login, MCP Auth（当指收 PU 账密）

**报名**:
向 PU 提交参加一场活动。本工具当场执行，不排队、不弹确认；用户在对话里点头后再调用。仅报名不计有效学分。
_Avoid_: 待抢, 自动报名, 加入待抢

**活动状态**:
PU 活动本身的人读状态（未开始/进行中/已结束），来自 `statusName`。不是能不能报名。
_Avoid_: 报名状态

**报名状态**:
报名窗口的人读状态（报名未开始/报名进行中/报名已结束）。list 用 `startTimeValue`；info 用 `buttonInfo` 或 `joinStartTime`/`joinEndTime`。`allow_signup` 表示现在能否报。
_Avoid_: 活动状态, allowJoinCount, joinStatus（用户是否已报，不是窗口）

**MCP**:
agent 调用本工具能力的接口，跑在本机。给 agent 访问 PU：认证状态、学校查询、列活动、活动详情、已报名、报名，以及各活动类型已签到次数。不收账密，不返回有效学分、必须补、总有效分、阶段线，不把公式写入工具描述，不提供待抢，不推荐场次。标准协议，不绑某一家客户端。PU 会话不出本机。
_Avoid_: 托管 MCP, 活动建议, 待抢, 已获（当作为 MCP 返回）

**skill**:
只提供登录配置：用学校查询查出学校 sid，告诉用户去跑 `pu login --sid …`，再用认证状态确认。不代填账密，不代跑带密码的命令。不写有效学分公式，不推荐场次。
_Avoid_: 复制 glossary 学分表, 活动推荐 skill, 在对话里收密码

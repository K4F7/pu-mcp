# MCP 只给 PU 访问，不待抢、不推荐

原先 MCP 保存待抢，并用「还能刷哪类」帮 agent 选活动。现改为：本工具不保存待抢，不推荐场次；何时报、报哪场由 agent 自己决定。报名是当场向 PU 提交。到点调度和提醒不在本工具。ADR-0006 的不自建 daemon 仍成立；`pu signup run {plan_id}` 不再是产品入口。ADR-0007 里「按工具结果选活动 / 还能刷哪类」作废，学分查询是否还暴露另定。

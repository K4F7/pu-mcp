# MCP 查询当前二课学分，不另写 skill

单独 skill 里的学分表会过期，agent 还要先读文件再调工具。改为 MCP 工具直接返回当前已获、必须补、总有效分、阶段线和还能刷的活动类型；agent 按这份动态结果选活动。规则仍以 glossary 为准，运行时以工具结果为准。

已被 ADR-0010 收回：MCP 不再返回已获、必须补、总有效分、阶段线和还能刷哪类。已被 ADR-0015 收窄：学分/选活动仍不写 skill；登录配置可以写 skill。

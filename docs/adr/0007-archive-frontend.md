# 网页进 archive_frontend，主线只留 CLI / MCP / 一次执行

选活动、待办不再做页面。静态网页、学分手填页、提醒日历、前端原型、`pu serve` 以及绑在它上面的到点循环调度，整包放到 git 分支 `archive_frontend`。主线保留登录、活动、待抢存储、一次执行报名、有效分、CLI，以及后续 MCP 与 skill。网页 JSON API 不单独保留。

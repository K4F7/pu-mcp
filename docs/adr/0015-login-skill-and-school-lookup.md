# 登录配置用 skill；学校 sid 用查询工具

`pu login` 仍只收学校 sid、账号、密码，不收中文校名。另提供未登录可调的学校查询（MCP + CLI），按校名或关键字返回学校 sid。登录配置 skill 只帮查学校 sid：先查、用户确认学校，再让用户自己跑 `pu login --sid …`，然后用 `auth_status` 确认。不代填账密，不代跑带密码的命令。不写学分公式，不推荐场次。ADR-0007「完全不写 skill」改为：学分/选活动不写 skill；登录配置可以写。学校列表接口以实现时 PU 未登录可调的为准。
